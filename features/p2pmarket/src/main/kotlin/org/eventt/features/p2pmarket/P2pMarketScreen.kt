package org.eventt.features.p2pmarket

import androidx.compose.runtime.Composable

// Phase 1: just Browse. My Orders / My Requests / Inbox arrive in later phases as real sub-tabs —
// not stubbed here, since a tab row with three dead tabs would be worse than no tab row at all.
@Composable
fun P2pMarketScreen() {
    BrowseScreen()
}
